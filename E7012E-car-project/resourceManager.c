#include "resourceManager.h"

float newAngle;
float newSpeed;
float velocity;

//struct SensorStatus current_sensorStatus;
//struct SensorStatus previous_sensorStatus;

void resourceManager_startup()
{
	newAngle = 0;
	newSpeed = 0;
	velocity = 0;
}
